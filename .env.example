# Database URL Configuration
# For SQLite (default, 0-config local testing):
DATABASE_URL="file:./dev.db"

# For MySQL (production / MySQL server):
# DATABASE_URL="mysql://root:password@localhost:3306/onestop_db"

# Application Branding Config
NEXT_PUBLIC_APP_NAME="One-Stop"
NEXT_PUBLIC_APP_TAGLINE="Your Future. One Step Ahead."
NEXT_PUBLIC_SIH_ID="SIH 1781"

# Admin Email Notification Service (Nodemailer / SMTP)
# Set your target admin email to receive new user registration alerts:
ADMIN_EMAIL="admin@onestopadvisor.com"

# SMTP Settings (Configure with Gmail, SendGrid, Amazon SES, Brevo, or Mailtrap)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="One-Stop Career Advisor <notifications@onestopadvisor.com>"
